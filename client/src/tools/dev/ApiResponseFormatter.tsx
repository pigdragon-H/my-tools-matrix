import { useMemo, useState } from "react";
export default function ApiResponseFormatter(){const [input,setInput]=useState('{"status":"ok","data":{"name":"工具矩陣","count":30}}');const result=useMemo(()=>{try{return JSON.stringify(JSON.parse(input),null,2)}catch(e){return e instanceof Error?`JSON 錯誤：${e.message}`:"JSON 錯誤"}},[input]);return <div className="mx-auto max-w-4xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">API 回應格式化工具</h1><p className="text-sm text-slate-600 dark:text-slate-300">貼上 JSON API 回應，即時格式化並檢查語法錯誤。</p><textarea value={input} onChange={e=>setInput(e.target.value)} className="min-h-40 w-full rounded-lg border p-3 font-mono"/><pre className="min-h-40 whitespace-pre-wrap rounded-xl bg-slate-50 p-4 font-mono text-sm dark:bg-slate-900">{result}</pre></div>}
