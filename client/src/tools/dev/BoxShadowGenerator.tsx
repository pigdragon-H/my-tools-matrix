import { useMemo, useState } from "react";
export default function BoxShadowGenerator(){const [x,setX]=useState(0),[y,setY]=useState(12),[blur,setBlur]=useState(30),[spread,setSpread]=useState(0),[opacity,setOpacity]=useState(20);const css=useMemo(()=>`${x}px ${y}px ${blur}px ${spread}px rgba(15, 23, 42, ${opacity/100})`,[x,y,blur,spread,opacity]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">Box Shadow 產生器</h1><p className="text-sm text-slate-600 dark:text-slate-300">調整陰影位移、模糊、擴散與透明度，取得 CSS box-shadow。</p><div className="grid gap-4 md:grid-cols-5">{[["X",x,setX],["Y",y,setY],["模糊",blur,setBlur],["擴散",spread,setSpread],["透明度%",opacity,setOpacity]].map(([l,v,s])=><label key={String(l)} className="text-sm">{String(l)}<input type="number" value={Number(v)} onChange={e=>(s as (n:number)=>void)(Number(e.target.value))} className="w-full rounded-lg border p-2"/></label>)}</div><div className="grid place-items-center rounded-xl bg-slate-100 p-12"><div className="rounded-xl bg-white p-10" style={{boxShadow:css}}>預覽陰影</div></div><pre className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900">box-shadow: {css};</pre></div>}
