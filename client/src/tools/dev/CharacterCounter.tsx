import { useMemo, useState } from "react";
export default function CharacterCounter(){const [text,setText]=useState("快速統計字元數、空白與標點。");const s=useMemo(()=>({all:text.length,noSpace:text.replace(/\s/g,"").length,spaces:(text.match(/\s/g)||[]).length,lines:text?text.split("\n").length:0}),[text]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">字元計數器</h1><p className="text-sm text-slate-600 dark:text-slate-300">統計總字元、不含空白字元、空白數與行數。</p><textarea value={text} onChange={e=>setText(e.target.value)} className="min-h-48 w-full rounded-lg border p-3"/><div className="grid gap-4 md:grid-cols-4"><b>總字元 {s.all}</b><b>不含空白 {s.noSpace}</b><b>空白 {s.spaces}</b><b>行數 {s.lines}</b></div></div>}
