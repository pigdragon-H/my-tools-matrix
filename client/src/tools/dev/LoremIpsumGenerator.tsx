import { useMemo, useState } from "react";
const words="工具 矩陣 公式 宇宙 效率 設計 開發 財務 健康 資料 分析 內容 平台 智慧 自動化".split(" ");export default function LoremIpsumGenerator(){const [count,setCount]=useState(80);const text=useMemo(()=>Array.from({length:count},(_,i)=>words[i%words.length]).join(" ")+"。",[count]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">假文產生器</h1><p className="text-sm text-slate-600 dark:text-slate-300">產生繁體中文測試文案，適合版面與 UI 測試。</p><label>字詞數<input type="number" value={count} onChange={e=>setCount(Number(e.target.value))} className="ml-3 rounded-lg border p-2"/></label><textarea readOnly value={text} className="min-h-48 w-full rounded-lg border p-3"/></div>}
