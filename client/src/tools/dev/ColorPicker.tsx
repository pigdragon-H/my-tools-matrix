import { useMemo, useState } from "react";
export default function ColorPicker(){const [color,setColor]=useState("#3b82f6");const rgb=useMemo(()=>{const h=color.replace("#","");const r=parseInt(h.slice(0,2),16),g=parseInt(h.slice(2,4),16),b=parseInt(h.slice(4,6),16);return {r,g,b,css:`rgb(${r}, ${g}, ${b})`};},[color]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><div><h1 className="text-2xl font-bold">色彩選擇器</h1><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">選取顏色並即時取得 HEX、RGB 與 Tailwind 設計參考。</p></div><input type="color" value={color} onChange={e=>setColor(e.target.value)} className="h-20 w-full rounded-lg"/><div className="h-28 rounded-xl border" style={{backgroundColor:color}}/><div className="grid gap-3 md:grid-cols-3"><pre className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900">HEX\n{color}</pre><pre className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900">RGB\n{rgb.css}</pre><pre className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900">數值\nR {rgb.r} / G {rgb.g} / B {rgb.b}</pre></div></div>}
