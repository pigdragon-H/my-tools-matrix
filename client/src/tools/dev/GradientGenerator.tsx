import { useMemo, useState } from "react";
export default function GradientGenerator(){const [from,setFrom]=useState("#06b6d4");const [to,setTo]=useState("#8b5cf6");const [deg,setDeg]=useState(135);const css=useMemo(()=>`linear-gradient(${deg}deg, ${from}, ${to})`,[from,to,deg]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">漸層產生器</h1><p className="text-sm text-slate-600 dark:text-slate-300">調整起訖顏色與角度，產生可直接貼上的 CSS 漸層。</p><div className="grid gap-4 md:grid-cols-3"><label>起始色<input type="color" value={from} onChange={e=>setFrom(e.target.value)} className="h-12 w-full"/></label><label>結束色<input type="color" value={to} onChange={e=>setTo(e.target.value)} className="h-12 w-full"/></label><label>角度<input type="number" value={deg} onChange={e=>setDeg(Number(e.target.value))} className="w-full rounded-lg border p-2"/></label></div><div className="h-40 rounded-xl border" style={{background:css}}/><pre className="whitespace-pre-wrap rounded-xl bg-slate-50 p-4 dark:bg-slate-900">background: {css};</pre></div>}
