import { useMemo, useState } from "react";
export default function BarcodeGenerator(){const [code,setCode]=useState("MTX20260522");const bars=useMemo(()=>Array.from(code).flatMap(ch=>{const n=ch.charCodeAt(0);return [n%5+1,n%3+1]}),[code]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">條碼產生器</h1><p className="text-sm text-slate-600 dark:text-slate-300">依輸入字串產生可視化條碼預覽，適合標籤版面測試。</p><input value={code} onChange={e=>setCode(e.target.value)} className="w-full rounded-lg border p-3"/><div className="flex h-32 items-end gap-1 rounded-xl bg-white p-6 shadow-inner">{bars.map((w,i)=><div key={i} className="bg-black" style={{width:`${w*3}px`,height:`${70+(i%4)*10}px`}} />)}</div><code>{code}</code></div>}
