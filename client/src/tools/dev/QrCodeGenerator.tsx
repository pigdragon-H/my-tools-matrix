import { useMemo, useState } from "react";
export default function QrCodeGenerator(){const [text,setText]=useState("https://mytoolsmatrix.com");const url=useMemo(()=>`https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=${encodeURIComponent(text)}`,[text]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">QR Code 產生器</h1><p className="text-sm text-slate-600 dark:text-slate-300">輸入網址或文字，即時產生 QR Code 圖片。</p><input value={text} onChange={e=>setText(e.target.value)} className="w-full rounded-lg border p-3"/><div className="rounded-xl bg-slate-50 p-6 text-center dark:bg-slate-900"><img src={url} alt="QR Code" className="mx-auto"/><p className="mt-3 break-all text-sm">{text}</p></div></div>}
