import { useMemo, useState } from "react";
export default function SslChecker(){const [domain,setDomain]=useState("mytoolsmatrix.com");const [days,setDays]=useState(90);const r=useMemo(()=>{const expire=new Date(Date.now()+days*86400000);return {valid:/^[a-z0-9.-]+\.[a-z]{2,}$/i.test(domain),expire:expire.toLocaleDateString("zh-TW"),status:days>30?"正常":days>7?"即將到期":"高風險"}},[domain,days]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">SSL 憑證檢查器</h1><p className="text-sm text-slate-600 dark:text-slate-300">輸入網域與剩餘天數，模擬檢查 SSL 憑證狀態。</p><div className="grid gap-4 md:grid-cols-2"><input value={domain} onChange={e=>setDomain(e.target.value)} className="rounded-lg border p-3"/><input type="number" value={days} onChange={e=>setDays(Number(e.target.value))} className="rounded-lg border p-3"/></div><div className="rounded-xl bg-emerald-50 p-5 dark:bg-emerald-950"><p>網域有效：{r.valid?"是":"否"}</p><p>到期日：{r.expire}</p><p>狀態：{r.status}</p></div></div>}
