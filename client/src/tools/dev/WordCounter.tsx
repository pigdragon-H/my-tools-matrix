import { useMemo, useState } from "react";
export default function WordCounter(){const [text,setText]=useState("工具矩陣 Formula Universe helps creators build useful calculators.");const stats=useMemo(()=>{const words=(text.trim().match(/[A-Za-z0-9]+|[\u4e00-\u9fff]/g)||[]).length;const lines=text?text.split("\n").length:0;return {words,lines,chars:text.length}},[text]);return <div className="mx-auto max-w-3xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">字數統計器</h1><p className="text-sm text-slate-600 dark:text-slate-300">統計中英文文字的字數、字元數與行數。</p><textarea value={text} onChange={e=>setText(e.target.value)} className="min-h-48 w-full rounded-lg border p-3"/><div className="grid gap-4 md:grid-cols-3"><b>字數：{stats.words}</b><b>字元：{stats.chars}</b><b>行數：{stats.lines}</b></div></div>}
