import { useMemo, useState } from "react";
export default function DiffChecker(){const [a,setA]=useState("第一行\n第二行\n第三行"),[b,setB]=useState("第一行\n第二行已修改\n第三行");const rows=useMemo(()=>{const aa=a.split("\n"),bb=b.split("\n"),n=Math.max(aa.length,bb.length);return Array.from({length:n},(_,i)=>({i,left:aa[i]??"",right:bb[i]??"",same:(aa[i]??"")===(bb[i]??"")}))},[a,b]);return <div className="mx-auto max-w-5xl space-y-6 rounded-2xl border bg-white p-6 shadow-sm dark:bg-slate-950"><h1 className="text-2xl font-bold">文字差異比對器</h1><p className="text-sm text-slate-600 dark:text-slate-300">逐行比對兩段文字，快速找出不同內容。</p><div className="grid gap-4 md:grid-cols-2"><textarea value={a} onChange={e=>setA(e.target.value)} className="min-h-40 rounded-lg border p-3 font-mono"/><textarea value={b} onChange={e=>setB(e.target.value)} className="min-h-40 rounded-lg border p-3 font-mono"/></div><div className="space-y-2">{rows.map(r=><div key={r.i} className={`grid gap-2 rounded-lg p-2 md:grid-cols-2 ${r.same?"bg-emerald-50":"bg-rose-50"}`}><code>{r.left}</code><code>{r.right}</code></div>)}</div></div>}
