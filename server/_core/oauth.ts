/**
 * oauth.ts — Manus OAuth Handler (DEPRECATED)
 *
 * This file is kept as an empty stub.
 * Authentication has been migrated to Supabase Auth.
 * See server/lib/supabaseAdmin.ts for the new auth implementation.
 */

import type { Express } from "express";

export function registerOAuthRoutes(_app: Express): void {
  // No-op: Manus OAuth has been replaced by Supabase Auth
}
